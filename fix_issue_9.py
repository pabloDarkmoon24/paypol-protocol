```json
{
  "solution_code": "### File: agents/dao-treasury-reporter/agent.py\n\n```python\n#!/usr/bin/env python3\n\"\"\"\nDAO Treasury Reporting Agent\nRegistered on PayPol marketplace - generates periodic treasury reports\nwith holdings breakdown, spending trends, and runway analysis.\n\"\"\"\n\nimport os\nimport json\nimport time\nimport logging\nimport hashlib\nfrom datetime import datetime, timedelta, timezone\nfrom typing import Any\n\nimport requests\nfrom flask import Flask, request, jsonify\nfrom web3 import Web3\nfrom web3.middleware import geth_poa_middleware\nfrom dotenv import load_dotenv\n\nload_dotenv()\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s [%(levelname)s] %(name)s: %(message)s\",\n)\nlog = logging.getLogger(\"treasury-agent\")\n\n# ---------------------------------------------------------------------------\n# Configuration\n# ---------------------------------------------------------------------------\n\nRPC_URL = os.getenv(\"TEMPO_RPC_URL\", \"https://rpc.tempo.xyz\")\nCHAIN_ID = int(os.getenv(\"TEMPO_CHAIN_ID\", \"42431\"))\nPAYPOL_API = os.getenv(\"PAYPOL_API_URL\", \"https://api.paypol.xyz\")\nPAYPOL_API_KEY = os.getenv(\"PAYPOL_API_KEY\", \"\")\nAGENT_WALLET = os.getenv(\"AGENT_WALLET_ADDRESS\", \"\")\nAGENT_PRIVATE_KEY = os.getenv(\"AGENT_PRIVATE_KEY\", \"\")\nPRICE_API = os.getenv(\"PRICE_API_URL\", \"https://api.coingecko.com/api/v3\")\nPORT = int(os.getenv(\"PORT\", \"8080\"))\n\n# Treasury contracts to monitor (comma-separated addresses from env)\nTREASURY_ADDRESSES = [\n    addr.strip()\n    for addr in os.getenv(\n        \"TREASURY_ADDRESSES\",\n        \"0x0000000000000000000000000000000000000001\",\n    ).split(\",\")\n    if addr.strip()\n]\n\n# Token configuration: address -> {symbol, decimals, coingecko_id, category}\nTOKEN_CONFIG: dict[str, dict] = json.loads(\n    os.getenv(\n        \"TOKEN_CONFIG\",\n        json.dumps(\n            {\n                \"0xAlphaUSD\": {\n                    \"symbol\": \"AlphaUSD\",\n                    \"decimals\": 18,\n                    \"coingecko_id\": \"usd-coin\",\n                    \"category\": \"Stablecoin\",\n                },\n                \"0xpathUSD\": {\n                    \"symbol\": \"pathUSD\",\n                    \"decimals\": 18,\n                    \"coingecko_id\": \"dai\",\n                    \"category\": \"Stablecoin\",\n                },\n                \"native\": {\n                    \"symbol\": \"TEMPO\",\n                    \"decimals\": 18,\n                    \"coingecko_id\": \"ethereum\",\n                    \"category\": \"Governance\",\n                },\n            }\n        ),\n    )\n)\n\n# Spending category keyword mapping (lowercase tx data / address labels)\nSPEND_CATEGORIES = {\n    \"payroll\": [\"payroll\", \"salary\", \"compensation\", \"contributor\"],\n    \"grants\": [\"grant\", \"bounty\", \"funding\", \"award\"],\n    \"ops\": [\"ops\", \"infrastructure\", \"tooling\", \"audit\", \"legal\", \"marketing\"],\n}\n\n# Minimal ERC-20 ABI\nERC20_ABI = json.loads(\n    \"\"\"\n[\n  {\"constant\":true,\"inputs\":[{\"name\":\"_owner\",\"type\":\"address\"}],\n   \"name\":\"balanceOf\",\"outputs\":[{\"name\":\"balance\",\"type\":\"uint256\"}],\n   \"type\":\"function\"},\n  {\"constant\":true,\"inputs\":[],\"name\":\"decimals\",\n   \"outputs\":[{\"name\":\"\",\"type\":\"uint8\"}],\"type\":\"function\"},\n  {\"constant\":true,\"inputs\":[],\"name\":\"symbol\",\n   \"outputs\":[{\"name\":\"\",\"type\":\"string\"}],\"type\":\"function\"},\n  {\"anonymous\":false,\n   \"inputs\":[\n     {\"indexed\":true,\"name\":\"from\",\"type\":\"address\"},\n     {\"indexed\":true,\"name\":\"to\",\"type\":\"address\"},\n     {\"indexed\":false,\"name\":\"value\",\"type\":\"uint256\"}\n   ],\n   \"name\":\"Transfer\",\"type\":\"event\"}\n]\n\"\"\"\n)\n\n# ---------------------------------------------------------------------------\n# Web3 setup\n# ---------------------------------------------------------------------------\n\nw3 = Web3(Web3.HTTPProvider(RPC_URL))\nw3.middleware_onion.inject(geth_poa_middleware, layer=0)\n\n\ndef _checksum(addr: str) -> str:\n    \"\"\"Return checksummed address or raise.\"\"\"\n    try:\n        return Web3.to_checksum_address(addr)\n    except Exception:\n        raise ValueError(f\"Invalid address: {addr}\")\n\n\n# ---------------------------------------------------------------------------\n# Price feed\n# ---------------------------------------------------------------------------\n\n_price_cache: dict[str, tuple[float, float]] = {}  # id -> (price, timestamp)\nPRICE_CACHE_TTL = 300  # 5 minutes\n\n\ndef get_price_usd(coingecko_id: str) -> float:\n    \"\"\"Fetch USD price from CoinGecko with simple caching.\"\"\"\n    now = time.time()\n    if coingecko_id in _price_cache:\n        price, ts = _price_cache[coingecko_id]\n        if now - ts < PRICE_CACHE_TTL:\n            return price\n\n    try:\n        url = f\"{PRICE_API}/simple/price\"\n        resp = requests.get(\n            url,\n            params={\"ids\": coingecko_id, \"vs_currencies\": \"usd\"},\n            timeout=10,\n        )\n        resp.raise_for_status()\n        data = resp.json()\n        price = float(data.get(coingecko_id, {}).get(\"usd\", 1.0))\n    except Exception as exc:\n        log.warning(\"Price fetch failed for %s: %s\", coingecko_id, exc)\n        price = 1.0  # fallback for stablecoins\n\n    _price_cache[coingecko_id] = (price, now)\n    return price\n\n\n# ---------------------------------------------------------------------------\n# Balance queries\n# ---------------------------------------------------------------------------\n\n\ndef get_native_balance(address: str) -> int:\n    \"\"\"Return native balance in wei.\"\"\"\n    try:\n        return w3.eth.get_balance(_checksum(address))\n    except Exception as exc:\n        log.error(\"Failed to get native balance for %s: %s\", address, exc)\n        return 0\n\n\ndef get_token_balance(token_address: str, wallet_address: str) -> int:\n    \"\"\"Return ERC-20 token balance in raw units.\"\"\"\n    try:\n        contract = w3.eth.contract(\n            address=_checksum(token_address), abi=ERC20_ABI\n        )\n        return contract.functions.balanceOf(_checksum(wallet_address)).call()\n    except Exception as exc:\n        log.error(\n            \"Failed to get token balance %s for %s: %s\",\n            token_address,\n            wallet_address,\n            exc,\n        )\n        return 0\n\n\ndef aggregate_holdings() -> dict:\n    \"\"\"\n    Aggregate all token holdings across all treasury addresses.\n    Returns dict with token breakdown and totals.\n    \"\"\"\n    holdings: dict[str, dict] = {}\n\n    for treasury_addr in TREASURY_ADDRESSES:\n        # Native balance\n        native_cfg = TOKEN_CONFIG.get(\"native\", {})\n        if native_cfg:\n            raw = get_native_balance(treasury_addr)\n            decimals = native_cfg.get(\"decimals\", 18)\n            symbol = native_cfg.get(\"symbol\", \"TEMPO\")\n            amount = raw / (10 ** decimals)\n            price = get_price_usd(native_cfg.get(\"coingecko_id\", \"ethereum\"))\n            usd_value = amount * price\n\n            if symbol not in holdings:\n                holdings[symbol] = {\n                    \"symbol\": symbol,\n                    \"amount\": 0.0,\n                    \"usd_value\": 0.0,\n                    \"price_usd\": price,\n                    \"category\": native_cfg.get(\"category\", \"Other\"),\n                    \"coingecko_id\": native_cfg.get(\"coingecko_id\", \"\"),\n                }\n            holdings[symbol][\"amount\"] += amount\n            holdings[symbol][\"usd_value\"] += usd_value\n\n        # ERC-20 tokens\n        for token_addr, cfg in TOKEN_CONFIG.items():\n            if token_addr == \"native\":\n                continue\n            raw = get_token_balance(token_addr, treasury_addr)\n            decimals = cfg.get(\"decimals\", 18)\n            symbol = cfg.get(\"symbol\", token_addr[:8])\n            amount = raw / (10 ** decimals)\n            price = get_price_usd(cfg.get(\"coingecko_id\", \"usd-coin\"))\n            usd_value = amount * price\n\n            if symbol not in holdings:\n                holdings[symbol] = {\n                    \"symbol\": symbol,\n                    \"amount\": 0.0,\n                    \"usd_value\": 0.0,\n                    \"price_usd\": price,\n                    \"category\": cfg.get(\"category\", \"Other\"),\n                    \"coingecko_id\": cfg.get(\"coingecko_id\", \"\"),\n                }\n            holdings[symbol][\"amount\"] += amount\n            holdings[symbol][\"usd_value\"] += usd_value\n\n    total_usd = sum(h[\"usd_value\"] for h in holdings.values())\n\n    # Compute percentage\n    for sym in holdings:\n        pct = (holdings[sym][\"usd_value\"] / total_usd * 100) if total_usd else 0\n        holdings[sym][\"pct\"] = round(pct, 2)\n\n    return {\"tokens\": holdings, \"total_usd\": total_usd}\n\n\n# ---------------------------------------------------------------------------\n# Transaction history & spending analysis\n# ---------------------------------------------------------------------------\n\n\ndef fetch